{
  "name": "taskManager-cc",
  "version": "0.1.0",
  "description": "Chaincodes package for taskManager",
  "main": "./dist/src/index.js",
  "typings": "./dist/src/index.d.ts",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "clean": "rimraf dist client",
    "build": "npm run clean && npm run client:generate && tsc",
    "prepare": "npm run build",
    "test": "npm run build && mocha -r ts-node/register tests/*.spec.ts --reporter spec",
    "client:generate": "generate-controller-interface -c TaskManagerController"
  },
  "dependencies": {
    "yup": "^0.27.0",
    "reflect-metadata": "^0.1.13",
    "participant-cc": "^0.1.0",
    "@worldsibu/convector-core-model": "^1.3.0",
    "@worldsibu/convector-core-controller": "^1.3.0"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/chai-as-promised": "^7.1.0",
    "@types/mocha": "^5.2.6",
    "@types/node": "^10.12.5",
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "mocha": "^5.0.3",
    "rimraf": "^2.6.2",
    "ts-node": "^8.0.2"
  }
}
